export interface TelegramUpdate { update_id:number; message?:{message_id:number; text?:string; chat:{id:number}; from?:{id:number; username?:string; is_bot?:boolean}}; }
export class TelegramClient { constructor(private token:string, private fetcher: typeof fetch = fetch){} private url(method:string){ return `https://api.telegram.org/bot${this.token}/${method}`; } async getUpdates(offset:number, timeout=25): Promise<TelegramUpdate[]> { const res=await this.fetcher(this.url('getUpdates'),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({offset,timeout,allowed_updates:['message','callback_query']})}); if(!res.ok) throw new Error(`Telegram getUpdates failed ${res.status}`); const json=await res.json() as {ok:boolean; result:TelegramUpdate[]}; return json.result; } async sendMessage(chatId:string|number,text:string): Promise<void> { const res=await this.fetcher(this.url('sendMessage'),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({chat_id:chatId,text})}); if(!res.ok) throw new Error(`Telegram sendMessage failed ${res.status}`); } async testToken(): Promise<boolean> { const res=await this.fetcher(this.url('getMe')); return res.ok; } }
