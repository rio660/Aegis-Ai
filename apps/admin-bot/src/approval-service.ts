import type { LicenseFeature, Plan } from '@aegis/shared-types';
import { validateRequestCode } from '@aegis/licensing';
import type { BotStorage } from './bot-storage.js';
import { buildLicensePayload, issueActivationKey } from './license-generator.js';
export class ApprovalService { constructor(private storage:BotStorage){} receiveRequest(code:string, telegramUserId:string, telegramUsername:string|null){ const request=validateRequestCode(code); const existing=this.storage.requests.get(request.requestId); if(existing) return existing; const record={request,telegramUserId,telegramUsername,status:'PENDING' as const,receivedAt:new Date().toISOString()}; this.storage.requests.set(request.requestId,record); this.storage.addAudit('REQUEST_RECEIVED',{requestId:request.requestId,deviceHash:request.deviceHash.slice(0,12)},telegramUserId); return record; } approve(requestId:string, opts:{plan:Plan; features:LicenseFeature[]; expiresAt:string|null; adminTelegramId:string; reissue?:boolean}): {activationKey:string} { const rec=this.storage.requests.get(requestId); if(!rec) throw new Error('REQUEST_NOT_FOUND'); if(rec.status==='ISSUED' && !opts.reissue) throw new Error('DUPLICATE_APPROVAL_BLOCKED'); if(rec.status!=='PENDING' && !opts.reissue) throw new Error('REQUEST_NOT_PENDING'); const keyId=this.storage.secrets.activeKeyId; const privateKeyPem=this.storage.secrets.privateKeyPem; if(!keyId||!privateKeyPem) throw new Error('SIGNING_KEY_NOT_CONFIGURED'); const payload=buildLicensePayload({request:rec.request,telegramUserId:rec.telegramUserId,telegramUsername:rec.telegramUsername,plan:opts.plan,features:opts.features,expiresAt:opts.expiresAt,keyId}); const issued=issueActivationKey(payload,privateKeyPem); rec.status=opts.reissue?'REISSUED':'ISSUED'; this.storage.issued.set(payload.licenseId,{license:payload,activationKeyHash:issued.activationKeyHash,status:rec.status,issuedAt:payload.issuedAt,telegramUserId:rec.telegramUserId}); this.storage.addAudit(opts.reissue?'LICENSE_REISSUED':'LICENSE_ISSUED',{requestId,licenseId:payload.licenseId},opts.adminTelegramId); return {activationKey:issued.activationKey}; } deny(requestId:string, adminTelegramId:string){ const rec=this.storage.requests.get(requestId); if(!rec) throw new Error('REQUEST_NOT_FOUND'); rec.status='DENIED'; this.storage.denied.set(requestId,rec); this.storage.addAudit('REQUEST_DENIED',{requestId},adminTelegramId); } revoke(licenseId:string, reason:string, adminTelegramId:string){ const issued=this.storage.issued.get(licenseId); if(!issued) throw new Error('LICENSE_NOT_FOUND'); issued.status='REVOKED_LOCAL_RECORD'; const record={licenseId,telegramUserId:issued.telegramUserId,deviceHash:issued.license.deviceHash,revokedAt:new Date().toISOString(),reason,adminTelegramId}; this.storage.revoked.set(licenseId,record); this.storage.addAudit('LOCAL_REVOKE_RECORDED',{licenseId,reason},adminTelegramId); return record; } }
