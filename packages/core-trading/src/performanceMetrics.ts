export function performanceMetrics(pnls:number[]): {trades:number; netPnl:number; winrate:number; profitFactor:number} { const wins=pnls.filter(p=>p>0), losses=pnls.filter(p=>p<0); return {trades:pnls.length, netPnl:pnls.reduce((a,b)=>a+b,0), winrate:pnls.length?wins.length/pnls.length:0, profitFactor: Math.abs(losses.reduce((a,b)=>a+b,0)) ? wins.reduce((a,b)=>a+b,0)/Math.abs(losses.reduce((a,b)=>a+b,0)) : 0}; }
