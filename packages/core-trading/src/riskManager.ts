import type { TradingDecision } from '@aegis/shared-types';
import { enforceTradeSafety } from '@aegis/safety';
export interface RiskCalculation { entry:number; stopLoss:number|null; takeProfit:number|null; riskReward:number|null; maxRiskPerTrade:number; positionSize:number; decision:TradingDecision; }
export function calculateRisk(input:{accountEquity:number; riskPct:number; entry:number; stopLoss?:number|null; takeProfit?:number|null; decision:TradingDecision}): RiskCalculation { const stopLoss=input.stopLoss ?? null; const takeProfit=input.takeProfit ?? null; const riskPerUnit = stopLoss ? Math.abs(input.entry-stopLoss) : 0; const rewardPerUnit = takeProfit ? Math.abs(takeProfit-input.entry) : 0; const rr = riskPerUnit && rewardPerUnit ? rewardPerUnit/riskPerUnit : null; const safety = enforceTradeSafety({decision:input.decision, stopLoss, riskReward:rr}); return {entry:input.entry, stopLoss, takeProfit, riskReward:rr, maxRiskPerTrade:input.accountEquity*input.riskPct, positionSize:riskPerUnit ? (input.accountEquity*input.riskPct)/riskPerUnit : 0, decision:safety.decision}; }
